<p id="notice"><%= notice %></p>

<p>
  <%= @diary.created_at.to_formatted_s(:extra_short) %>
</p>
<p>
  <strong>Title:</strong>
  <%= @diary.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @diary.content %>
</p>

<button type="button" class="btn btn-success"><%= link_to 'Edit', edit_diary_path(@diary) %></button>
<button type="button" class="btn btn-primary"><%= link_to 'Back', diaries_path %></button>
